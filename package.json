{
  "name": "legalhub-law-cbt",
  "version": "7.0.0",
  "private": true,
  "description": "Legal Hub — computer-based testing platform for the UNEC 030 Law class",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "serve": "python3 -m http.server 8080 --bind 0.0.0.0"
  }
}
